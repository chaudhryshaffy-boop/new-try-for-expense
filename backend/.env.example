# Backend environment variables
ENV=development
TESTING=0

# Database
# For local Postgres via Docker Compose
DATABASE_URL=postgresql+psycopg2://postgres:postgres@localhost:5432/finance
# For SQLite local fallback
# DATABASE_URL=sqlite+pysqlite:///./finance.db

# Supabase Auth configuration
# Either provide JWKS URL or a JWT secret for local dev
SUPABASE_JWKS_URL=https://YOUR_PROJECT.supabase.co/auth/v1/keys
# SUPABASE_JWT_SECRET=your-local-jwt-secret

# CORS origins (comma-separated)
# BACKEND_CORS_ORIGINS=http://localhost:5173

# OpenAI API Key (optional for AI features)
OPENAI_API_KEY=